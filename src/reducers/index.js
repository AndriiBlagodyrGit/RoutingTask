import comments, { initialCommentsState } from './comments';
import posts, { initialPostsState } from './posts';
import userInfo, { initialUserInfoState } from './userInfo';
import filter, { initialFilterState } from './filter';

export {
  comments,
  initialCommentsState,
  posts,
  initialPostsState,
  userInfo,
  initialUserInfoState,
  filter,
  initialFilterState,
};
